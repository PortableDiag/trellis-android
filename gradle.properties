android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Build cache disabled: restoring cached task outputs fails to chmod files on the
# veracrypt volume ("Could not set file mode 700"). Recomputing is reliable here.
org.gradle.caching=false
android.enableJetifier=false
